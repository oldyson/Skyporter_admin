import { LoadingSkyporterDirective } from './loading-skyporter.directive';

describe('LoadingSkyporterDirective', () => {
	it('should create an instance', () => {
		const directive = new LoadingSkyporterDirective();
		expect(directive).toBeTruthy();
	});
});
